on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      docker_image_name:
        required: true
        type: string
      docker_image_tag:
        required: true
        type: string

jobs:
  deploy:
    timeout-minutes: 30
    name: Deploy WAS docker image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_SSH_USER }}
          key: ${{ secrets.SERVER_SSH_PRIVATE_KEY }}
          script: |
            cd ~/projects/team-unchanined/grantly
            echo "${{ secrets.PACKAGE_READ }}" | sudo -S docker login ghcr.io -u ${{ github.actor }} --password-stdin
            sudo docker pull ghcr.io/${{ inputs.docker_image_name }}:${{ inputs.docker_image_tag }}
            sudo docker stop api || true
            sudo docker rm api || true
            sudo docker run -d --name api -p 80:8080 ghcr.io/${{ inputs.docker_image_name }}:${{ inputs.docker_image_tag }}
            sudo docker image prune -a
